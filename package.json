{
  "name": "electron-sound",
  "version": "1.0.0",
  "author": "",
  "description": "",
  "main": "main.js",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-packager . electron-sound --platform=linux --arch=x64 --overwrite"
  },
  "devDependencies": {
    "electron": "^24.1.2",
    "electron-rebuild": "^3.2.9",
    "speaker": "^0.5.4",
    "wav": "^1.0.2"
  }
}
